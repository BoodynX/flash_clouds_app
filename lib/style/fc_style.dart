import 'package:flutter/material.dart';

class FcStyle {
  static const main = Colors.teal;
  static const bg = Colors.white;
  static const shadow = Colors.grey;
  static const select = Colors.amberAccent;
  static const delete = Color.fromRGBO(255, 205, 210, 1.0);
}
